#!/bin/bash

# e - exit on error
# r - restricted (as a security measure)
# x - verbose and expand commands
# u - error on unset variables
# n - read the commands but don't execute them (remove when ready to run)
set -enxur

# This is the bash file used for deployment
